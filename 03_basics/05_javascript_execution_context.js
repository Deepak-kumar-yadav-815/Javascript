/**
 * javascript is single theraded language
 * 
 * 
 */

/**
 * global exection context
 * function exection context
 * eval execution context
 * {} -> memory creation phase
 *    -> exection phase
 */
// see the attested image

/**
 * call stack():-
 * call stack = ["global execution",""]
 */