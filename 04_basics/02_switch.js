const month = "Deepek";
switch (month) {
    case 1: console.log(`the month is jan`);
        break;
    case 2: console.log(`the month is feb`);
        break;
    case "Deepak": console.log(`deepak`);
        break;
    default: console.log("bhai bhai");
        break;
}